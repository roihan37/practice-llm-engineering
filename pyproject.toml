[project]
name = "llm-testing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Roihan Salsabila", email = "pro.roihan@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "ipython>=8.18.1",
    "openai>=2.48.0",
    "python-dotenv>=1.2.1",
]

[project.scripts]
llm-testing = "llm_testing:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
